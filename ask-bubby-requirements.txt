# requirements.txt
flask==2.0.1
gunicorn==20.1.0
python-dotenv==0.19.0

# render.yaml
services:
  - type: web
    name: ask-bubby
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9
